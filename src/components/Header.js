import React from 'react';

// Create Header function component
const Header = () =>
(
    <header>
        <h1>React Photo Gallery App</h1>  
    </header>
);

// Export component
export default Header;