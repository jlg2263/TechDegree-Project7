import React from 'react';

// Create NoResults function component
const NoResults = () =>
(
    <li className="not-found">
        <h3>No Results Found</h3>
        <p>Your search did not return any results. Please try again.</p>
    </li>
);

// Export component
export default NoResults;
